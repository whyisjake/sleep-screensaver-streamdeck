name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Stream Deck CLI
        run: npm install -g @elgato/cli

      - name: Build plugin (prod)
        run: npm run build

      - name: Package plugin
        run: streamdeck pack com.whyisjake.sleep-screensaver.sdPlugin

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: sleep-screensaver-${{ github.ref_name }}
          path: com.whyisjake.sleep-screensaver.streamDeckPlugin
          if-no-files-found: error

      - name: Create GitHub release
        uses: softprops/action-gh-release@v1
        with:
          files: com.whyisjake.sleep-screensaver.streamDeckPlugin
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
